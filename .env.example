# Required
SDK_KEY=YOUR_DJI_APP_KEY

# Optional
PORT=8787
MAX_CONCURRENCY=2
PARSE_TIMEOUT_MS=180000

# Rate limiting
RATE_WINDOW_MS=60000
RATE_MAX=30
TRUST_PROXY=1

# Build-only (used by docker-compose.dev.yml)
DJI_REF=v1.0.6
